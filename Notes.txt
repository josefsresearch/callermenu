+ add update so it can add list of curNumbers whenever can, curNum on phone so dont make unnecessary calls
+ change lag, no main lag, but 2 lags, 2 time, 1 string
+ change model, just hash
+ 

++ call button on all last options
++ add call button so that the call is not made when button pressed, but when user makes the call
++ call button on bottom
++ Call icon on none
++ icon for Reservation()

++ addtab? so can have full menu and page by page mode. (full menu mode or individual mode)
++ AA - add flight info submit flight number, submit advantage account number





